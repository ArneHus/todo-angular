import { TodoCountListPipe } from './todo-count-list.pipe';

describe('TodoCountListPipe', () => {
  it('create an instance', () => {
    const pipe = new TodoCountListPipe();
    expect(pipe).toBeTruthy();
  });
});
